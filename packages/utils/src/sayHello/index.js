/**
 * @name sayHello
 * @description 输出输入的内容，如果不输入则输出Hello.....
 * @param {string} str - 内容
 * @returns {string}
 *
 * @example
 * sayHello('wellwell')
 * =>
 * 'wellwell'
 */

const sayHello = (str = 'Hello.....') => {
	return str;
};

export default sayHello;
